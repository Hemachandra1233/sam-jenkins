apiVersion: v1
kind: Pod
metadata:
  name: kaniko
spec:
  containers:
  - name: npm
    image: amazon/aws-sam-cli-build-image-provided
    command:
    - sh
    - -c
    - |
      apt-get update
      apt-get install -y nodejs npm
    tty: true
