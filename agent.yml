apiVersion: v1
kind: Pod
metadata:
  name: kaniko
spec:
  containers:
  - name: npm
    image: amazon/aws-sam-cli-build-image-provided
    command:
    - cat
    tty: true
  - name: python
    image: nikolaik/python-nodejs
    command:
    - /bin/cat
    tty: true    
